apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-claim
  namespace: k8s-wordpress-project
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 2Gi

